# Append ~/.bin to PATH
if [ -d "$HOME/.bin" ]; then
  export PATH="$PATH:$HOME/.bin"
fi

# Append ~/.bin to PATH
if [ -d "$HOME/bin" ]; then
  export PATH="$PATH:$HOME/bin"
fi

# Set vim as the default editor.
export EDITOR="vim"
